import { RouterModule, Routes } from '@angular/router';
import { EachVideoTimeComponent } from './each-video-time.component';

export const eachVideoTimeRoutes: Routes = [
    {
        path: '',
        component: EachVideoTimeComponent
    }
]