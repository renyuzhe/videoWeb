import { RouterModule, Routes } from '@angular/router';
import { MonthTimesComponent } from './month-times.component';

export const monthTimeRoutes: Routes = [
    {
        path: '',
        component: MonthTimesComponent
    }
]